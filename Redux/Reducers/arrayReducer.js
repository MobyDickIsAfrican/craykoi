import {ADD_UI} from '../Actions/ACTION_CONSTANTS.js';
/*
import {defaultArray} from './defaultState.js';

function arrayReducer(state = defaultArray, action){
    switch (action.type) {
        case ADD_UI:
            const arr = []
            for(let id of state){
                arr.push(id);
            };
            arr.push(action.payload.id);
            return arr;
        default:
            return state;
    }
};

export default arrayReducer;
*/